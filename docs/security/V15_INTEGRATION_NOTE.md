# V15 Integration Note

This integration adapts the isolated V12–V15 security/release packages into the MATRIX-LAB-SPORTS application namespace.

- `matrix_security` -> `app.security`
- `matrix_release` -> `app.release`
- tests -> `tests/security`

The integration is development/research governance only. It does not authorize production release, automatic model promotion, or automatic wagering.
